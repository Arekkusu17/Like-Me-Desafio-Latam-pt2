# Desafio 3-4 LikeMe Backend 

Made a few improvements since the last challenge, this one uses the pattern called Model-View-Controller

## Deployment

To use the project, first run:

```bash
  npm install
```
To install the dependencies of the package-lock.

**Don't forget to configure your own .env file following the example.**

Then run the server.


